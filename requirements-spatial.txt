# OPTIONAL NON-WINDOWS / ADVANCED FALLBACK ONLY
# ------------------------------------------------
# Do NOT use this file as the primary Windows install path.
# Windows users who need spatial analysis should create the Conda environment
# from environment.yml, activate it, and run run-conda.bat / .\run-conda.ps1.
#
# Why: pip installs on Windows can fall back to building Fiona/GDAL from source,
# which commonly fails with messages about GDAL_VERSION or gdal-config.
# The supported Windows path installs the full spatial stack from conda-forge.
#
# This file remains available for:
#   - Linux or macOS environments with working system GDAL tooling
#   - Advanced users who deliberately manage native geospatial dependencies

# Geospatial libraries
geopandas>=0.13.0
shapely>=2.0.0
pyproj>=3.5.0
rtree>=1.0.0
pyogrio>=0.7.0

# Interactive maps
folium>=0.14.0
